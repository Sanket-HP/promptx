{
  "name": "promptx-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "PromptX - Intelligent LLM Token Optimization Platform & TokenForge Gateway",
  "author": "PromptX Team",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "npm --workspace=packages/shared run build && npm --workspace=packages/token-engine run build && npm --workspace=packages/compression run build && npm --workspace=packages/optimizer run build && npm --workspace=packages/cache run build && npm --workspace=packages/pricing run build && npm --workspace=packages/providers run build && npm --workspace=packages/routing run build && npm --workspace=packages/sdk run build && npm --workspace=packages/database run build",
    "build:api": "npm run build:packages && npm --workspace=apps/api run build",
    "build:web": "npm run build:packages && npm --workspace=apps/web run build",
    "build": "npm run build:packages && npm --workspace=apps/api run build && npm --workspace=apps/web run build",
    "test": "npm run test --workspaces --if-present",
    "dev": "npm --workspace=apps/api run dev",
    "dev:web": "npm --workspace=apps/web run dev",
    "dev:api": "npm --workspace=apps/api run dev",
    "prisma:generate": "npm --workspace=packages/database run generate",
    "prisma:push": "npm --workspace=packages/database run push",
    "prisma:seed": "npm --workspace=packages/database run seed"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "@types/node": "^20.11.0"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "styled-jsx": "5.1.1",
    "postcss": "^8.4.31",
    "postcss-selector-parser": "^6.0.13",
    "postcss-nested": "^6.0.1",
    "postcss-js": "^4.0.1",
    "postcss-load-config": "^4.0.1",
    "postcss-value-parser": "^4.2.0",
    "autoprefixer": "^10.4.16",
    "picomatch": "^2.3.1",
    "micromatch": "^4.0.5",
    "normalize-path": "^3.0.0",
    "didyoumean": "^1.2.2",
    "dlv": "^1.1.3",
    "fast-glob": "^3.3.2",
    "glob-parent": "^6.0.2",
    "is-glob": "^4.0.3",
    "object-hash": "^3.0.0",
    "picocolors": "^1.0.0",
    "queue-microtask": "^1.2.3",
    "reusify": "^1.0.4",
    "resolve": "^1.22.8",
    "ts-interface-checker": "^0.1.13",
    "sucrase": "^3.35.0",
    "watchpack": "^2.4.0"
  },
  "overrides": {
    "styled-jsx": {
      "react": "$react"
    }
  }
}
